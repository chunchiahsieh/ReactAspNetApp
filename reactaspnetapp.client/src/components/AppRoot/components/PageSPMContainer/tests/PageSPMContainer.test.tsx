import {render} from '@testing-library/react';
import {PageSPMContainer} from '../PageSPMContainer';

describe('<PageSPMContainer />', () => {
    test('it should mount', () => {
        render(<PageSPMContainer />);
    });
});
