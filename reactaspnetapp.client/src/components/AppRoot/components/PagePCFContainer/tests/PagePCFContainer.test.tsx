import {render} from '@testing-library/react';
import {PagePCFContainer} from '../PagePCFContainer';

describe('<PagePCFContainer />', () => {
    test('it should mount', () => {
        render(<PagePCFContainer />);
    });
});
