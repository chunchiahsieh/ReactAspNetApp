import {render} from '@testing-library/react';
import {SupplierAuthorize} from '../SupplierAuthorize';

describe('<SupplierAuthorize />', () => {
    test('it should render', () => {
        render(<SupplierAuthorize />);
    });
});
