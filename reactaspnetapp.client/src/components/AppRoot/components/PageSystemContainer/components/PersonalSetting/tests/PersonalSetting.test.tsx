import {render} from '@testing-library/react';
import {PersonalSetting} from '../PersonalSetting';

describe('<PersonalSetting />', () => {
    test('it should render', () => {
        render(<PersonalSetting />);
    });
});
