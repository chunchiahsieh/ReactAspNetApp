import {render} from '@testing-library/react';
import {ESGAppBar} from '../ESGAppBar';

describe('<ESGAppBar />', () => {
    test('it should render', () => {
        render(<ESGAppBar />);
    });
});
