import {render} from '@testing-library/react';
import {PageMainContainer} from '../PageMainContainer';

describe('<PageMainContainer />', () => {
    test('it should mount', () => {
        render(<PageMainContainer />);
    });
});
