import {render} from '@testing-library/react';
import {CircleAnIcon} from '../CircleAnIcon';

describe('<CircleAnIcon />', () => {
    test('it should mount', () => {
        render(<CircleAnIcon />);
    });
});
