import {ColumnsFilterManager} from '../ColumnsFilterManager';
import {render} from '@testing-library/react';

describe('<ColumnsFilterManager />', () => {
    test('should render correctly', () => {
        render(<ColumnsFilterManager />);
    });
});
