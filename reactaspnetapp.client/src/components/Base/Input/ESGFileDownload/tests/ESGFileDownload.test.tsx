import {render} from '@testing-library/react';
import {ESGFileDownload} from '../ESGFileDownload';

describe('<ESGFileDownload />', () => {
    test('it should mount', () => {
        render(<ESGFileDownload />);
    });
});
