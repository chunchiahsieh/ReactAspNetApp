import {render} from '@testing-library/react';
import {ESGCheckbox} from '../ESGCheckbox';

describe('<ESGCheckbox />', () => {
    test('it should mount', () => {
        render(<ESGCheckbox />);
    });
});
