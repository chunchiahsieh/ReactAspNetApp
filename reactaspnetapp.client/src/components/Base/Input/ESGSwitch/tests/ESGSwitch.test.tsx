import {render} from '@testing-library/react';
import {ESGSwitch} from '../ESGSwitch';

describe('<ESGSwitch />', () => {
    test('it should render', () => {
        render(<ESGSwitch />);
    });
});
