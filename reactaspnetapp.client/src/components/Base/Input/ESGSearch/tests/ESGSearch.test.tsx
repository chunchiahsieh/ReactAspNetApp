import {render} from '@testing-library/react';
import {ESGSearch} from '../ESGSearch';

describe('<ESGSearch />', () => {
    test('it should render', () => {
        render(<ESGSearch />);
    });
});
