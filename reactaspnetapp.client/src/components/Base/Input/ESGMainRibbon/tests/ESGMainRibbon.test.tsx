import {render} from '@testing-library/react';
import {ESGMainRibbon} from '../ESGMainRibbon';

describe('<ESGMainRibbon />', () => {
    test('it should render', () => {
        render(<ESGMainRibbon />);
    });
});
