import {render} from '@testing-library/react';
import {ESGTextinput} from '../ESGTextinput';

describe('<ESGTextinput />', () => {
    test('it should render', () => {
        render(<ESGTextinput />);
    });
});
